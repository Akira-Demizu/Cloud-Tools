services:
  almalinux9:
    build:
      context: .
      dockerfile: ./docker/almalinux9/Dockerfile
    container_name: almalinux9
    volumes:
      - ./samples:/samples:ro
      - ./scripts:/scripts:ro
    tty: true